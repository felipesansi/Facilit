@model Facilit.Models.Usuario

@{
    ViewBag.Title = "Index";
}
@if (!String.IsNullOrEmpty(TempData["Mensagem"] as string))
{
<div id="mensagem">
    <div class="alert alert-danger">
        @TempData["Mensagem"]
    </div>
</div>
}

<script>
    setTimeout(function () {
        var caixa_mensagem = document.getElementById('mensagem');
        if (caixa_mensagem) {
            caixa_mensagem.style.display = 'none';

        }

    }, 5000);
</script>



<div class="container">
    <div class="row">
        <div class="col-md-6">

            <h2>Sistema Facilit</h2>
            <hr />

            @using (Html.BeginForm("VerificarLogin", "usuario"))
            {
                @Html.AntiForgeryToken()

                <div class="form-horizontal">
                    <h4>Seja bem-vindo(a) | Informe suas Credenciais</h4>
                    <br />
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                    <div class="form-group">
                        @Html.LabelFor(model => model.Nome_Usuario, " Usuário:", htmlAttributes: new { @class = "control-label col-md-6" })
                        <div class="col-md-10">
                            @Html.EditorFor(model => model.Nome_Usuario, new { htmlAttributes = new { @class = "campo" } })
                            @Html.ValidationMessageFor(model => model.Nome_Usuario, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.Senha_Usuario, "Senha:", htmlAttributes: new { @class = "control-label col-md-6" })
                        <div class="col-md-10">
                            @Html.PasswordFor(model => model.Senha_Usuario, new { @class = "campo" })

                            @Html.ValidationMessageFor(model => model.Senha_Usuario, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <p>
                        @Html.ActionLink("Esqueci minha senha", "RecuperarSenha", "Usuario",null, new { @class = "btn btn-secundary" })
                    </p>
                    <br />
                    <div class="form-group">
                       
                        <input type="submit" value="Login" class="btn btn-primary" />

                        

                        <br />

                    </div>
                </div>
                <br />
            }
        </div>


        <div class="col-md-6 text-right">
            <img src="~/imagens/imagem login.png" alt="Imagem ilústrativa de login" class="img-fluid" />
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
